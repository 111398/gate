-- Различаем сообщения онбординга/дообучения персоны от основного разговора,
-- чтобы основной чат их не показывал (см. обсуждение с пользователем после
-- первого прод-релиза: онбординг должен "исчезать" из истории после
-- прохождения, а дополнение персоны из настроек — не мешать основному чату).

alter table messages
  add column phase text not null default 'main' check (phase in ('onboarding', 'main'));
