export const SAFETY_LEVELS = ["none", "mild_distress", "acute_crisis"] as const;
export type SafetyLevel = (typeof SAFETY_LEVELS)[number];

// Хардкод RU-текста — временно, до подключения i18n-каркаса (этап 9), после
// которого контакты помощи переедут в словари next-intl и станут зависеть от
// локали пользователя (см. ТЗ п.6.5, п.9).
export const CRISIS_HOTLINE_NUMBER = "8-800-2000-122";

// Фиксированный, НЕ генерируемый LLM ответ — см. ТЗ п.6.5 и нефункциональное
// требование п.11: кризисная ветка не должна зависеть от того, что модель
// "правильно себя поведёт".
export const CRISIS_RESPONSE_TEXT = `Мне жаль, что тебе сейчас так тяжело — и я рада, что ты написал(а) об этом.

Пожалуйста, обратись за помощью прямо сейчас — это важно, и ты заслуживаешь поддержки. Бесплатная круглосуточная линия психологической помощи: ${CRISIS_HOTLINE_NUMBER} (анонимно, по всей России).

Если есть непосредственная опасность для жизни — позвони 112 или обратись к близкому человеку, который может быть рядом прямо сейчас.

Я — приложение-компаньон, а не специалист, и не могу заменить профессиональную помощь в такой ситуации.`;

// Мягкое напоминание при лёгком/умеренном дистрессе — периодически подмешивается
// в системный промпт персоны (не в каждом сообщении), см. buildPersonaSystemPrompt.
export const MILD_DISTRESS_REMINDER_PROBABILITY = 0.34;
